name: 🧪 Build and smoke test snap

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  build:
    name: 🔨 Build snap
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build snap
        uses: snapcore/action-build@v1
        id: build

      - name: Show log on build failure
        if: failure()
        run: cat /home/runner/.local/state/snapcraft/log/snapcraft*.log

      - name: Review snap
        uses: diddlesnaps/snapcraft-review-action@v1
        with:
          snap: ${{ steps.build.outputs.snap }}
          isClassic: 'false'

      - name: Upload snap artifact
        uses: actions/upload-artifact@v4
        with:
          name: snap
          path: ${{ steps.build.outputs.snap }}
          retention-days: 7

  smoke-test:
    name: 💨 Smoke test (launch + screenshot)
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download snap artifact
        uses: actions/download-artifact@v4
        with:
          name: snap

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Setup LXD
        uses: canonical/setup-lxd@v0.1.1

      - name: Setup ghvmctl
        run: |
          sudo snap install ghvmctl
          sudo snap connect ghvmctl:lxd lxd:lxd

      - name: Prepare VM
        run: ghvmctl prepare

      - name: Install snap into VM
        run: |
          SNAP_FILE=$(ls *.snap | head -1)
          echo "Pushing $SNAP_FILE into VM..."
          VM_NAME=$(ghvmctl exec -- hostname | tr -d '[:space:]')
          lxc file push "$SNAP_FILE" "local:$VM_NAME/home/ubuntu/$SNAP_FILE"
          ghvmctl exec -- sudo snap install --dangerous "/home/ubuntu/$SNAP_FILE"

      - name: Launch app and wait for it to render
        run: |
          ghvmctl snap-run dosbox-staging
          sleep 30

      - name: Take screenshots
        run: |
          ghvmctl screenshot-full
          ghvmctl screenshot-window
          ls -lh "$HOME/ghvmctl-screenshots/"

      - name: Output app logs
        if: always()
        run: |
          ghvmctl exec -- cat /home/ubuntu/dosbox-staging.dosbox-staging.log 2>/dev/null || true

      - name: Upload screenshots
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: smoke-test-screenshots
          path: ~/ghvmctl-screenshots/
          if-no-files-found: warn
          retention-days: 14

      - name: Comment on PR with screenshot link
        if: github.event_name == 'pull_request'
        continue-on-error: true
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const runUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `### 🧪 Smoke test ✅\n\nSnap launched successfully in a VM. 📸 Download the \`smoke-test-screenshots\` artifact from the [run](${runUrl}) to view screenshots.`
            });
